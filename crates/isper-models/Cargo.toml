[package]
name = "isper-models"
version = "0.1.0"
edition = "2024"

[dependencies]
ureq = { version = "2", features = ["json"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
tracing = "0.1"
# Pastas conhecidas do Windows (SHGetKnownFolderPath) — ver models_dir().
dirs = "5"
